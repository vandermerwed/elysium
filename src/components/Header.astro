---
import { SITE } from "@config";

export interface Props {
  activeNav?: "writing" | "projects" | "about" | "search" | "notes" | "journal" | "tags" | "archives";
}

const { activeNav } = Astro.props;
const currentPath = Astro.url.pathname;
const isActive = (path: string, navKey?: Props["activeNav"]) =>
  (navKey && activeNav === navKey) || currentPath.startsWith(path);
---

<header>
  <a id="skip-to-content" href="#main-content">Skip to content</a>
  <div class="nav-container">
    <div class="top-nav-wrap">
      <a href="/" class="logo" aria-label="Home">
        <span class="logo-mark" aria-hidden="true">~/</span><span class="logo-cursor" aria-hidden="true">_</span>
      </a>
      <nav id="nav-menu">
        <button
          class="hamburger-menu focus-outline"
          aria-label="Open Menu"
          aria-expanded="false"
          aria-controls="menu-items"
        >
          <svg
            xmlns="http://www.w3.org/2000/svg"
            width="24"
            height="24"
            viewBox="0 0 24 24"
            fill="none"
            stroke="currentColor"
            stroke-width="1.5"
            stroke-linecap="round"
            stroke-linejoin="round"
            class="menu-icon"
          >
            <line x1="7" y1="12" x2="21" y2="12" class="line"></line>
            <line x1="3" y1="6" x2="21" y2="6" class="line"></line>
            <line x1="12" y1="18" x2="21" y2="18" class="line"></line>
            <line x1="18" y1="6" x2="6" y2="18" class="close"></line>
            <line x1="6" y1="6" x2="18" y2="18" class="close"></line>
          </svg>
        </button>
        <ul id="menu-items" class="display-none sm:flex">
          <li>
            <a href="/notes/" class={isActive("/notes", "notes") ? "active" : ""}>
              /notes
            </a>
          </li>
          <li>
            <a href="/writing/" class={isActive("/writing", "writing") ? "active" : ""}>
              /writing
            </a>
          </li>
          <li>
            <a href="/journal/" class={isActive("/journal", "journal") ? "active" : ""}>
              /journal
            </a>
          </li>
          <li>
            <a href="/projects/" class={isActive("/projects", "projects") ? "active" : ""}>
              /projects
            </a>
          </li>
          <li>
            <a href="/about/" class={isActive("/about", "about") ? "active" : ""}>
              /about
            </a>
          </li>
          <li class="nav-icon-item">
            <a
              href="/search/"
              class={`focus-outline nav-icon-link ${isActive("/search", "search") ? "active" : ""}`}
              aria-label="Search"
              title="Search"
            >
              <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round">
                <circle cx="11" cy="11" r="7" />
                <line x1="16.65" y1="16.65" x2="21" y2="21" />
              </svg>
              <span class="sr-only">Search</span>
            </a>
          </li>
          {
            SITE.lightAndDarkMode && (
              <li class="nav-icon-item">
                <button
                  id="theme-btn"
                  class="focus-outline"
                  title="Toggles light & dark"
                  aria-label="auto"
                  aria-live="polite"
                >
                  <svg xmlns="http://www.w3.org/2000/svg" id="moon-svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round">
                    <path d="M21 12.79A9 9 0 1 1 11.21 3 7 7 0 0 0 21 12.79z" />
                  </svg>
                  <svg xmlns="http://www.w3.org/2000/svg" id="sun-svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round">
                    <circle cx="12" cy="12" r="5" />
                    <line x1="12" y1="1" x2="12" y2="3" />
                    <line x1="12" y1="21" x2="12" y2="23" />
                    <line x1="4.22" y1="4.22" x2="5.64" y2="5.64" />
                    <line x1="18.36" y1="18.36" x2="19.78" y2="19.78" />
                    <line x1="1" y1="12" x2="3" y2="12" />
                    <line x1="21" y1="12" x2="23" y2="12" />
                    <line x1="4.22" y1="19.78" x2="5.64" y2="18.36" />
                    <line x1="18.36" y1="5.64" x2="19.78" y2="4.22" />
                  </svg>
                </button>
              </li>
            )
          }
        </ul>
      </nav>
    </div>
  </div>
  <div class="header-rule">
    <hr aria-hidden="true" />
  </div>
</header>

<style>
  #skip-to-content {
    @apply absolute -top-full left-16 z-50 bg-skin-accent px-3 py-2 text-skin-inverted transition-all focus:top-4;
  }
  .nav-container {
    @apply mx-auto flex max-w-3xl flex-col items-center justify-between sm:flex-row;
  }
  .top-nav-wrap {
    @apply relative flex w-full items-center justify-between px-4 py-4 sm:py-6;
    min-height: 64px;
  }
  .logo {
    @apply flex items-center font-mono text-lg font-medium no-underline;
    color: rgb(var(--color-text-base));
    background: none !important;
  }
  .logo:hover {
    text-decoration: none;
    background: none !important;
  }
  .logo-mark {
    @apply font-mono text-sm;
    color: rgb(var(--color-accent));
  }
  .logo-cursor {
    @apply font-mono text-sm;
    color: rgb(var(--color-accent));
    opacity: 0;
    transition: opacity 0.15s;
  }
  .logo:hover .logo-cursor {
    animation: blink 1s step-end infinite;
  }
  @keyframes blink {
    0%, 100% { opacity: 1; }
    50% { opacity: 0; }
  }
  .hamburger-menu {
    @apply self-center p-2 sm:hidden;
  }
  .hamburger-menu svg {
    @apply h-6 w-6 scale-125 fill-skin-base;
  }

  nav {
    @apply flex w-full flex-col items-center sm:ml-2 sm:flex-row sm:justify-end sm:space-x-4 sm:py-0;
  }
  nav ul {
    @apply mt-4 grid w-44 grid-cols-2 grid-rows-6 gap-x-2 gap-y-2 sm:ml-0 sm:mt-0 sm:flex sm:w-auto sm:items-center sm:gap-1;
  }
  nav ul li {
    @apply col-span-2 flex items-center justify-center;
  }
  nav ul li a {
    @apply w-full px-4 py-3 text-center font-mono text-sm sm:my-0 sm:px-2 sm:py-1;
    color: rgb(var(--color-base));
    text-decoration: none;
    transition: color var(--transition-base);
  }
  nav ul li a:hover {
    color: rgb(var(--color-text-base));
    text-decoration: none;
  }
  nav a.active {
    color: rgb(var(--color-text-base));
    font-weight: 500;
  }

  /* Icon items (search, theme) */
  .nav-icon-item {
    @apply col-span-1;
  }
  .nav-icon-link {
    @apply flex items-center justify-center p-3 sm:p-1;
    background: none !important;
  }
  .nav-icon-link svg {
    @apply h-4 w-4;
    color: rgb(var(--color-base));
    fill: none;
    stroke: currentColor;
  }
  .nav-icon-link:hover svg,
  .nav-icon-link.active svg {
    color: rgb(var(--color-accent));
  }

  nav button {
    @apply p-1;
  }
  nav button svg {
    @apply h-4 w-4;
    color: rgb(var(--color-base));
    fill: none;
    stroke: currentColor;
  }
  nav button:hover svg {
    color: rgb(var(--color-accent));
  }
  #theme-btn {
    @apply p-3 sm:p-1;
  }

  .menu-icon line {
    @apply transition-opacity duration-75 ease-in-out;
  }
  .menu-icon .close {
    opacity: 0;
  }
  .menu-icon.is-active .line {
    @apply opacity-0;
  }
  .menu-icon.is-active .close {
    @apply opacity-100;
  }

  .header-rule {
    @apply mx-auto max-w-3xl px-4;
  }
  .header-rule hr {
    border-color: rgb(var(--color-border));
    border-top-width: 1px;
  }
</style>

<script>
  function toggleNav() {
    // Toggle menu
    const menuBtn = document.querySelector(".hamburger-menu");
    const menuIcon = document.querySelector(".menu-icon");
    const menuItems = document.querySelector("#menu-items");

    menuBtn?.addEventListener("click", () => {
      const menuExpanded = menuBtn.getAttribute("aria-expanded") === "true";
      menuIcon?.classList.toggle("is-active");
      menuBtn.setAttribute("aria-expanded", menuExpanded ? "false" : "true");
      menuBtn.setAttribute(
        "aria-label",
        menuExpanded ? "Open Menu" : "Close Menu"
      );
      menuItems?.classList.toggle("display-none");
    });
  }

  toggleNav();

  // Runs on view transitions navigation
  document.addEventListener("astro:after-swap", toggleNav);
</script>
